name: First Workflow
on: workflow_dispatch
jobs: 
  first-job:
    runs-on: ubuntu-latest
    steps: 
      - name: Print Greeting
        run: | 
             echo "Hello World!"
             echo "This is my application"
      - name: Print Good Bye
        run: echo "Done -bye!"
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Ouput GitHub Context
        run: echo "${{github}}"